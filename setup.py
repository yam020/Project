#!/usr/bin/env python

"""
Install decision_maker pacakage. To install locally use:
	`pip install -e .`
"""

from setuptools import setup

setup(
	name = "NYCCIC",
	version = "0.0.1",
	packages = ['']
)